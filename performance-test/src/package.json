{
  "type": "module",
  "dependencies": {
    "@azure/cosmos": "^3.17.3"
  },
  "scripts" : {
    "start-precondition" : "export $(cat .env | xargs) && node ./pre_condition_script.js",
    "start-teardown" : "export $(cat .env | xargs) && node ./teardown_script.js"
  }
}
